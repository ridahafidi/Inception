version: "3.9"

services:
  nginx:
    build: ./requirements/nginx
    container_name: nginx
    ports:
      - "443:443"
    networks:
      - inception
    restart: unless-stopped
networks:
  inception:
    driver: bridge